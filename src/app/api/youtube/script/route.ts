import { NextResponse } from "next/server";
export async function GET() { return NextResponse.json({ status: "coming_soon", message: "Backend is being rebuilt." }); }
export async function POST() { return NextResponse.json({ status: "coming_soon", message: "Backend is being rebuilt." }); }

